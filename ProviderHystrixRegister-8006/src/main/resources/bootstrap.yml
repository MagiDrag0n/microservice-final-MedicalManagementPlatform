spring:
  cloud:
    config:
      discovery:
        service-id: config-center-7601
        enabled: true
      uri: http://127.0.0.1:7601
      label: master
      profile: dev
      name: provider-hystrix-register-8006

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka
